super=default
import=common/terraframe.properties

environment=develop

local.bin=${local.root}/bin
local.src=${deploy.webinf}/source
#local.lib=${local.root}/lib
#local.ant=${local.root}/scripts/ant/deploy

local.classpath=${deploy.root}/lib/credentialsSingleton.jar

server.src=${deploy.webinf}/source/server
server.bin=${local.bin}
server.lib=${local.lib}

client.src=${deploy.webinf}/source/client
client.bin=${local.bin}
client.lib=${local.lib}

common.src=${deploy.webinf}/source/common
common.bin=${local.bin}
common.lib=${local.lib}

log.dir=${local.root}/logs/

#webapp.root=${local.root}/webapp
jsp.dir=${deploy.webinf}

session.cache=${deploy.webinf}/sessionCache/

#web.dir=${deploy.path}/webDir/

# Additional aspect path for server compilation, comma delimited
#serverAspectPath=${server.lib}/mdssaspect.jar
