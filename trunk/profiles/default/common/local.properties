import=common/terraframe.properties
import=common/deploy.properties

environment=develop

local.bin=${local.root}/bin
local.src=${local.root}/src
local.lib=${local.root}/lib
local.ant=${local.root}/scripts/ant/deploy

local.classpath=

server.src=${generated.root}/server
server.bin=${generated.root}/server/bin
server.lib=${local.lib}/server

client.src=${generated.root}/client
client.bin=${generated.root}/client/bin
client.lib=${local.lib}/client

common.src=${generated.root}/common
common.bin=${generated.root}/common/bin
common.lib=${local.lib}/common

log.dir=${local.root}/logs/

webapp.root=${local.root}/webapp
jsp.dir=${webapp.root}/WEB-INF

session.cache=sessionCache/
permission.cache=${deploy.webinf}/permissionCache/

web.dir=${deploy.path}/webDir/

# Additional aspect path for server compilation, comma delimited
serverAspectPath=${server.lib}/mdssaspect.jar